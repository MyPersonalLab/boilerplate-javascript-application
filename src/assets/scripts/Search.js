

/**
 * 
 * @param {Object} userModel Object to access data from database 
 * @param {String} query QueryString to find data into the userModel
 */
function findData(userModel, query) {
    return //...
}